/** Non-sensitive build identity exposed only via ?navcapture=1 diagnostics. */
export const BUILD_SHA = process.env.NEXT_PUBLIC_BUILD_SHA ?? "unknown";
export const MICRO_SLIDE_IMPL_VERSION = "micro-slide-activation-v10";
